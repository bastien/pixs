<ul id="sortingOptions">
  <li id="sortIMDB" class="first active"><a href="#">IMDB rating</a></li>
  <li id="sortAZ"><a href="#">Alphabetically</a></li>
  <li id="sortCalendar"><%= link_to "Calendar", list_calendar_path(@list) %></li>
</ul>

<ul id="filmList">
  <%= render @films %>
</ul>

<div id='actions'>
<%= link_to "+ Add a movie", new_list_film_path(@list) if current_user && @list.user == current_user %>
</div>